{
  "name": "webhook-monitor-telegram",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generic webhook retry monitor with Telegram alerts for project webhooks.",
  "main": "src/index.mjs",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test test/*.test.mjs",
    "smoke": "node src/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.0"
  }
}
